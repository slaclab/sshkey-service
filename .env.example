# SSH Key Service Configuration Example
# Copy this file to .env and adjust the values for your environment

# Redis Configuration
SLACSSH_REDIS_HOST=dragonfly
SLACSSH_REDIS_PORT=6379
SLACSSH_REDIS_PASSWORD=
SLACSSH_REDIS_DB=0

# Authentication
# Header field that contains the authenticated username
SLACSSH_USERNAME_HEADER_FIELD=REMOTE-USER

# Key Validity
# Time in seconds that a key remains valid (default: 90000 seconds = 25 hours)
SLACSSH_VALIDITY_PERIOD=90000

# Email Notification Configuration
# Set to 'true' to enable email notifications when users register SSH keys
SLACSSH_EMAIL_ENABLED=false

# SMTP Server Settings
SLACSSH_SMTP_HOST=localhost
SLACSSH_SMTP_PORT=587
SLACSSH_SMTP_USER=
SLACSSH_SMTP_PASSWORD=
SLACSSH_SMTP_FROM=noreply@sshkey-service.local
SLACSSH_SMTP_USE_TLS=true

# Email Domain
# Domain to append to usernames to form email addresses (e.g., '@slac.stanford.edu')
# If username already contains '@', this is ignored
SLACSSH_EMAIL_DOMAIN=

# Example configurations for common email providers:

# Gmail:
# SLACSSH_SMTP_HOST=smtp.gmail.com
# SLACSSH_SMTP_PORT=587
# SLACSSH_SMTP_USER=your-email@gmail.com
# SLACSSH_SMTP_PASSWORD=your-app-password
# SLACSSH_SMTP_FROM=your-email@gmail.com
# SLACSSH_SMTP_USE_TLS=true

# Office 365/Outlook:
# SLACSSH_SMTP_HOST=smtp.office365.com
# SLACSSH_SMTP_PORT=587
# SLACSSH_SMTP_USER=your-email@yourdomain.com
# SLACSSH_SMTP_PASSWORD=your-password
# SLACSSH_SMTP_FROM=your-email@yourdomain.com
# SLACSSH_SMTP_USE_TLS=true

# AWS SES:
# SLACSSH_SMTP_HOST=email-smtp.us-west-2.amazonaws.com
# SLACSSH_SMTP_PORT=587
# SLACSSH_SMTP_USER=your-ses-smtp-username
# SLACSSH_SMTP_PASSWORD=your-ses-smtp-password
# SLACSSH_SMTP_FROM=noreply@yourdomain.com
# SLACSSH_SMTP_USE_TLS=true